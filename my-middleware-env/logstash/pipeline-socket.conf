input {
    tcp {
        port => 12000
        codec => json
        mode => server
    }
}

filter {
    
}

output {
    elasticsearch {
        hosts => ["env-es:9200"]
        index => "logstash-logs-%{+YYYY.MM.dd}"
        user => "elastic"
        password => "elastic"
    }
}